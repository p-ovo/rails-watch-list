<div>
  <%= simple_form_for(@list) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Submit List", class: "btn btn-primary" %>

  <% end %>
</div>
