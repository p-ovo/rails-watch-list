<h2>
<%= @list.name %>
</h2>

<br>
<h3>
<% counter = 1 %>
Bookmarks:<br>
<% @list.bookmarks.each do |bookmark|%>

<%= counter %>. <%= bookmark.comment %>
<% counter = counter + 1 %>
<% end %>
</h3>

<%= link_to 'Back', lists_path %> | <%= link_to 'Add new bookmark', new_list_bookmark_path(@list.id) %>
